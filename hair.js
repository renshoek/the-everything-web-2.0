  // Get the container element
  var container = document.getElementById('hair');

  // Create a new image element
  var img = document.createElement('img');
  img.src = 'images/hair.png';

  // Set a random position for the image
  img.style.position = 'absolute';
  img.style.left = Math.random() * 100 + '%';
  img.style.top = Math.random() * 100 + '%';

  // Add the image to the container
  container.appendChild(img);

  // Get the container element
  var container = document.getElementById('hair2');

  // Create a new image element
  var img = document.createElement('img');
  img.src = 'images/hair.png';

  // Set a random position for the image
  img.style.position = 'absolute';
  img.style.left = Math.random() * 100 + '%';
  img.style.top = Math.random() * 100 + '%';

  // Add the image to the container
  container.appendChild(img);

  // Get the container element
  var container = document.getElementById('hair3');

  // Create a new image element
  var img = document.createElement('img');
  img.src = 'images/hair.png';

  // Set a random position for the image
  img.style.position = 'absolute';
  img.style.left = Math.random() * 100 + '%';
  img.style.top = Math.random() * 100 + '%';

  // Add the image to the container
  container.appendChild(img);

  // Get the container element
  var container = document.getElementById('hair4');

  // Create a new image element
  var img = document.createElement('img');
  img.src = 'images/hair.png';

  // Set a random position for the image
  img.style.position = 'absolute';
  img.style.left = Math.random() * 100 + '%';
  img.style.top = Math.random() * 100 + '%';

  // Add the image to the container
  container.appendChild(img);

  // Get the container element
  var container = document.getElementById('hair5');

  // Create a new image element
  var img = document.createElement('img');
  img.src = 'images/hair.png';

  // Set a random position for the image
  img.style.position = 'absolute';
  img.style.left = Math.random() * 100 + '%';
  img.style.top = Math.random() * 100 + '%';

  // Add the image to the container
  container.appendChild(img);

  // Get the container element
  var container = document.getElementById('hair6');

  // Create a new image element
  var img = document.createElement('img');
  img.src = 'images/hair.png';

  // Set a random position for the image
  img.style.position = 'absolute';
  img.style.left = Math.random() * 100 + '%';
  img.style.top = Math.random() * 100 + '%';

  // Add the image to the container
  container.appendChild(img);