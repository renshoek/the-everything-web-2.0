$(document).on('mousemove', (event) => {
  $('#followbox').css({
    left: event.clientX,
    top: event.clientY,
  });
});

$(document).on('mousemove', (event) => {
  $('#box1').css({
    left: event.clientX,
    top: event.clientY,
  });
});

$(document).on('mousemove', (event) => {
  $('#box2').css({
    left: event.clientX,
    top: event.clientY,
  });
});

$(document).on('mousemove', (event) => {
  $('#box3').css({
    left: event.clientX,
    top: event.clientY,
  });
});